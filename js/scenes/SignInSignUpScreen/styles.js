import { StyleSheet } from 'react-native';
import { windowHeight, windowWidth } from '../../styles/baseStyles'
import {} from './../../styles/baseStyles'

export const styles = StyleSheet.create({
	hello: {
		height: windowHeight,
		width: windowWidth,
		alignItems: 'center',
		justifyContent: 'center',
		backgroundColor: '#FFF'
	}
});